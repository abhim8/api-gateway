services:
  api-gateway:
    build: .
    container_name: api-gateway
    ports:
      - "8000:8000"
    environment:
      - DEFAULT_LOG_LEVEL=${DEFAULT_LOG_LEVEL:-INFO}
      - GATEWAY_CORS_ORIGINS=${GATEWAY_CORS_ORIGINS:-https://app.example.com}
    restart: unless-stopped
